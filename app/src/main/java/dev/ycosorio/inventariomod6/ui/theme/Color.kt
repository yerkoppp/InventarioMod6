package dev.ycosorio.inventariomod6.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal (Morado del Logo)
val InventarisPurple40 = Color(0xFF7F52C4) // Primario (Modo Claro)
val InventarisPurple80 = Color(0xFFD8BFFF) // Primario (Modo Oscuro)

// Paleta Secundaria (Verde del Logo)
val InventarisGreen40 = Color(0xFF4CAF50) // Secundario (Modo Claro)
val InventarisGreen80 = Color(0xFFB9F6CA) // Secundario (Modo Oscuro)

// Colores "A juego" (Terciarios / Acentos)
val InventarisComplement40 = Color(0xFF006874) // Terciario (Modo Claro)
val InventarisComplement80 = Color(0xFF4CD9EC) // Terciario (Modo Oscuro)

// Paleta Neutral (Fondos y Contenedores)
val Neutral40 = Color(0xFF605D62)
val Neutral80 = Color(0xFFCAC4D0)